<?xml version="1.0" encoding="UTF-8"?>
<queries>
    <select>
        SELECT * FROM cliente
    </select>
    <select>
        SELECT * FROM cliente WHERE id_dpersona = 2
    </select>
    <update>
        
    </update>
    <insert>
        INSERT INTO cliente (id_dpersona, numepers, nombpers, ape1pers, ape2pers, tipodocu, numedocu, fechnaci, sexo, pais, idioma, id_dpedomi_fk, fechalta) 
        VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)
    </insert>
    <delete>
        
    </delete>
</queries>